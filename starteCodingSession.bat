
// startet alle autohotkeys für die geöffnete session

// coding session = {
      const hotkeysArray = Array.from(hotkeys.sets.powertoys);
      // hotkeys muss evtl. manuell vergeben werden ( eingefügt werden )
      hotKeysArray.forEach((hotkey) {
      windows.openFile(hotkey)
      }


      console.log("primär ursprünglich von colorPicker , ausgeartet in screensppliter , bildschirmanpassung(=> weitere optimierungs , anwendungsbeispiele für ahk)");
};
